name: Pure.DOM
description: |
  The application bootstrap for server and client applications.
excerpt: |
  Exports the core *inject* function for constructing and managing client and server applications.

  <pre data-try>
  import Pure

  main = inject body "Hello, World!"
  </pre>
